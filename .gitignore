# Dependencies (dev-only: Puppeteer for screenshot tests)
node_modules/
# Node manifests kept OUT of the deploy so Hostinger treats this as a
# static site and does not attempt an npm build (which fails on Puppeteer).
package.json
package-lock.json

# Screenshot test scripts & their output
shot*.js
shot_*.png
debug_markers.js

# Working scratch / source captures (not part of the published site)
my_slices/
ref_slices/
Home2First.org Photos/
www.charitywater.org_*.png
Chase Field 2026.jpeg
charitywater-original.html

# OS / editor cruft
.DS_Store
**/.DS_Store
.vscode/
.idea/

# Env / secrets
.env
.env.*

# Logs & build/cache output
*.log
dist/
.cache/
